// https://docs.asciidoctor.org/asciidoc/latest/lists/continuation/

// -- Multiline principal text --

* The document header in AsciiDoc is optional.
If present, it must start with a document title.

* Optional author and revision information lines
immediately follow the document title.

* The document header must be separated from
  the remainder of the document by one or more
  empty lines and it cannot contain empty lines.

// -- Attach blocks using list continuation --

* The header in AsciiDoc must start with a document title.
+
The header is optional.

// with listing block

* The header in AsciiDoc must start with a document title.
+
----
= Document Title
----
+
Keep in mind that the header is optional.

* Optional author and revision information lines immediately follow the document title.
+
----
= Document Title
Doc Writer <doc.writer@asciidoc.org>
v1.0, 2022-01-01
----

// using open block wrapper

* The header in AsciiDoc must start with a document title.
+
--
Here's an example of a document title:

----
= Document Title
----

NOTE: The header is optional.
--

// -- Drop principal text --

. {empty}
+
----
print("one")
----
. {empty}
+
----
print("two")
----

// NB: not supported in asciidork (yet, at least)
// syntax is discouraged and descrbed as "fragile" in the docs

// -- Ancestor list continuation --

// parent level

// * parent list item
// ** child list item
//
// +
// paragraph attached to parent list item
//
// // grandparent level
//
// * grandparent list item
// ** parent list item
// *** child list item
//
//
// +
// paragraph attached to grandparent list item
